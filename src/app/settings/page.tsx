import Link from "next/link";
import { ArrowRight, Bell, Building2, FileCog, Goal, NotebookPen, Tags } from "lucide-react";
import { Card } from "@/components/ui";

const settings = [["Brands & channels", "Account details, logos, managers and channel connections", "/settings/brands", Building2], ["Diagnostic rules", "Transparent thresholds, sample floors and evidence requirements", "/settings/diagnostics", FileCog], ["Tags", "Per-brand content taxonomy and tag-level analysis", "#", Tags], ["Goals", "Targets, progress, projections and status", "#", Goal], ["Alert defaults", "Recommended rule templates and notification channels", "/alerts", Bell], ["Internal notes", "Never included in client reports unless explicitly opted in", "#", NotebookPen]] as const;
export default function SettingsPage() { return <div className="space-y-7"><div><h1 className="page-title">Settings</h1><p className="page-subtitle">Control account scope, deterministic business rules and internal-only workflow defaults.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{settings.map(([title, detail, href, Icon]) => <Link href={href} key={title}><Card className="flex h-full items-start gap-4 p-5 transition hover:border-violet-200"><div className="grid size-10 shrink-0 place-items-center rounded-xl bg-[var(--surface)]"><Icon size={18} /></div><div className="flex-1"><p className="font-semibold">{title}</p><p className="mt-1 text-sm leading-6 text-[var(--muted)]">{detail}</p></div><ArrowRight size={16} className="mt-1 text-[var(--muted)]" /></Card></Link>)}</div><Card className="border-amber-200 bg-amber-50/40 p-5"><p className="font-semibold">Internal notes safety</p><p className="mt-1 text-sm leading-6 text-[var(--muted)]">Brand, video, report and opportunity notes are marked “Internal — never included in client reports” by default. Export requires an explicit opt-in.</p></Card></div>; }
